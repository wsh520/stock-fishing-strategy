# Data source (only one, hard dependency). akshare includes pandas/requests/tqdm/lxml and other transitive dependencies.
akshare>=1.12.0,<2.0.0

# Calculation and data structures. Add major version upper limits: CI uses pip install -r without constraints to pull latest,
# pandas 3.x has breaking changes compared to 2.x (default StringDtype, etc.),
# upper limits prevent next major version upgrade from directly breaking scheduled tasks.
numpy>=1.24.0,<3.0.0
pandas>=2.0.0,<4.0.0

# Feishu Webhook notification
requests>=2.28.0,<3.0.0

